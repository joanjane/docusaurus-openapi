import type { Preset, LoadContext } from "@docusaurus/types";
import type { Options } from "docusaurus-preset-openapi";
export default function preset(context: LoadContext, options?: Options): Preset;
